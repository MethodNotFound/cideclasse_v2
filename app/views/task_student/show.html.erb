<h1 class="text-3xl font-bold text-center mb-8"><%= @task.title %></h1>

<%= @task.problem %>

<%= @task.submissions.where(student: @student).count %>

<%= link_to "Enviar trabalho", new_submission_path(task_id: @task.id) %>
